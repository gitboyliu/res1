package com.forezp.web;

/**
 * copyright(c) 2019
 * FileName:text
 * Author:  ynFrineds
 * Description:测试方法主类
 * create :2019/3/26
 */
public class text {

    public static void main(String[] args) {
        //to do
        //int [] arr=new int[10];
        //arr[0]=10;arr[1]=5; arr[2]=5; arr[3]=6;arr[4]=8;
        //arr[5]=5; arr[6]=10;arr[7]=11;arr[8]=5;arr[9]=12;
        //数字，次数
        //List<Map<Integer,Integer>> mapList=new ArrayList<Map<Integer,Integer>>();

        //List<Integer> listNumber=new ArrayList<Integer>();

        //List<Integer> listCount=new ArrayList<Integer>();

       /* for (int i=0;i<arr.length;i++){
            //获取arr[i]
            int num=arr[i];
            //出现的次数
            int numb_count=0;
            //数字
            Integer numbers=null;

            //判断是否已经存在
            if (listNumber.contains(num)==false){
                for (int j=0;j<arr.length;j++){
                    if (num==arr[j]){
                        numb_count++;
                    }
                }
                numbers=num;
            }

            if (numbers!=null){
                listNumber.add(numbers);
                listCount.add(numb_count);
            }
        }*/
        /*//数字：
        System.out.println(listNumber.toString());

        //次数
        System.out.println(listCount.toString());

        //输出
        */
        /*for (int i=0;i<listNumber.size();i++){
            System.out.println(listNumber.get(i)+":"+listCount.get(i));
        }*/
    }
}
